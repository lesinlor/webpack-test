# webpack-test app
this is a webpack bundle and dev server test

running in vagrant.

# install package
`$ npm install`

# bulid the js bundle
`$ npm run bulid`

# run the webpack-dev-server
`$ npm run start`
